import 'package:flutter_riverpod/flutter_riverpod.dart';

class SignInService {
  SignInService(this.ref);
  final Ref ref;

  // fetch the user data from local or remote repository
  // depending on the network state
}
